Function TODO:

createTimers
==> Each player should have a separate timer
==> a timer can start, trigger function when finish
==> reset when finish
==> reset

Timer()
=> Time (default 1mn?)
=> Play(callback)
=> Reset()

Create single file for all ERRORS_MESSAGES

https://bulldogjob.com/news/449-how-to-write-a-good-readme-for-your-github-project

Play(assets)
activeSection = getActiveSection(history, board, mode)
if => checkIfTileBelongToSection(tile, activeSection) => OK
if => checkIfWon(activeSection) => sectionsState[activeSection].push()/checkIfWon(sectionsState)
history.push(tile)
activePlayer = !Player
return {updatedAssets}
