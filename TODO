Function TODO:

createTimers
==> Each player should have a separate timer
==> a timer can start, trigger function when finish
==> reset when finish
==> reset

Timer()
=> Time (default 1mn?)
=> Play(callback)
=> Reset()

Create single file for all ERRORS_MESSAGES

https://bulldogjob.com/news/449-how-to-write-a-good-readme-for-your-github-project

Play(assets)
if => checkIfWon(activeSection) => sectionStates[activeSection].push()/checkIfWon(sectionStates)
history.push(tile)
activePlayer = !Player
return {updatedAssets}

assets.winner should be a sectionState
